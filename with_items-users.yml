---
 - hosts: 54.242.211.51
   user: root
   tasks:
   - name: create cis-group
     group: name=cis-group gid=111
   - name: create 3 users
     user:
       name: "{{ item.name}}"
       group: "{{ item.group}}"
       password: "{{ item.password}}"
     with_items:
     - {name: 'user1', group: 'cis-group', password: 'priya'}
     - {name: 'user2', group: 'cis-group', password: 'hasini'}
     - {name: 'user3', group: 'cis-group', password: 'cuty'}


